# План разработки: Web Content Analyzer

## 🎯 Цель проекта

Создать "хакерскую" утилиту для автоматизации сбора и анализа контента с различных сайтов:
- Сканирование сайтов (Telegram, Pinterest, любой другой)
- Сбор контента + метаданных
- Анализ данных (паттерны, дубликаты, связи)
- Генерация отчётов

**Навыки, которые покажет проект:**
- ✅ Асинхронное программирование (asyncio, aiohttp)
- ✅ HTML парсинг (BeautifulSoup)
- ✅ Архитектура кода (модульность, расширяемость)
- ✅ Анализ данных (статистика, паттерны, отчёты)
- ✅ CLI интерфейс (argparse)

---

## 📁 Целевая архитектура проекта

```
async_url_collector/
├── config.py              # Настройки проекта
├── __main__.py            # CLI точка входа
│
├── downloader/            # Модуль скачивания
│   ├── __init__.py
│   ├── core.py           # Основная логика (async, semaphore, retries)
│   ├── io_utils.py        # Чтение/запись файлов (уже есть)
│   └── manifest.py        # Сохранение метаданных в JSONL
│
├── parsers/               # Парсеры для разных платформ
│   ├── __init__.py
│   ├── base.py           # Базовый класс парсера (исправить!)
│   ├── generic.py        # Универсальный парсер для любого сайта
│   ├── pinterest.py      # Парсер Pinterest
│   └── telegram.py       # Парсер Telegram
│
├── analyzer/              # НОВОЕ: Модуль анализа данных
│   ├── __init__.py
│   ├── duplicates.py     # Поиск дубликатов по хешу
│   ├── metadata.py       # Анализ метаданных
│   ├── patterns.py       # Поиск паттернов
│   └── reporter.py       # Генерация отчётов
│
└── utils/                 # Вспомогательные функции
    ├── __init__.py
    └── exif.py           # Извлечение EXIF (опционально)
```

---

## 📚 Этап 1: Базовая инфраструктура (Неделя 1)

### 🎓 Что изучать:

1. **`asyncio.Semaphore`** (30 минут)
   - Что это: ограничитель параллелизма
   - Зачем: не перегружать сервер множеством одновременных запросов
   - Как работает: `async with semaphore: ...`
   - Ресурсы: документация Python, примеры на Stack Overflow

2. **`aiofiles`** (30 минут)
   - Что это: асинхронная работа с файлами
   - Зачем: не блокировать event loop при записи на диск
   - Базовое использование: `async with aiofiles.open(...) as f:`
   - Ресурсы: документация aiofiles на GitHub

3. **JSONL формат** (15 минут)
   - Что это: JSON Lines - каждая строка = отдельный JSON объект
   - Зачем: удобно для логов, можно добавлять по одной строке
   - Пример: `{"url": "..."}\n{"url": "..."}\n`
   - Ресурсы: описание формата JSONL

4. **`pathlib.Path`** (20 минут)
   - Что это: современный способ работы с путями
   - Зачем: кроссплатформенность (Windows/Linux)
   - Базовые операции: `/`, `.parent`, `.exists()`, `.mkdir()`
   - Ресурсы: документация Python pathlib

### ✍️ Что писать:

#### 1.1. Исправить существующий код

**Файл: `parsers/base.py`**
- ✅ Исправить опечатку: строка 50 `'html,parser'` → `'html.parser'`
- ✅ Улучшить фильтрацию расширений (учитывать URL с параметрами `?v=123`)

**Файл: `main.py`**
- ✅ Добавить импорт: `from downloader.downloader import download_one`
- ✅ Раскомментировать и исправить определение `out_dir`
- ✅ Исправить вызов `crawl_user_illusts()` - либо читать из файла, либо передать URL

**Файл: `requirements.txt`**
- ✅ Добавить `beautifulsoup4` (если ещё нет)

#### 1.2. Создать `config.py`

**Файл: `async_url_collector/config.py`**

Структура:
```python
from pathlib import Path

# Пути
PROJECT_ROOT = Path(__file__).parent.parent
DOWNLOADS_DIR = PROJECT_ROOT / "downloads"
DATA_DIR = PROJECT_ROOT / "async_url_collector" / "data"

# Настройки загрузчика
DOWNLOADER_CONFIG = {
    "concurrency": 8,      # Количество одновременных загрузок
    "timeout": 30,         # Таймаут запроса в секундах
    "retries": 3,          # Количество повторных попыток
    "retry_delay": 1,      # Задержка между попытками (секунды)
}

# Настройки парсера
PARSER_CONFIG = {
    "allowed_extensions": [".jpg", ".jpeg", ".png", ".gif", ".webp"],
    "user_agent": "Mozilla/5.0 ...",
}

# Настройки манифеста
MANIFEST_CONFIG = {
    "filename": "manifest.jsonl",
}
```

**Что делать:**
- Создать файл с этими настройками
- Использовать `pathlib.Path` для путей
- Добавить комментарии

#### 1.3. Создать `downloader/manifest.py`

**Файл: `async_url_collector/downloader/manifest.py`**

Функции для реализации:

1. **`save_to_manifest(results, manifest_path)`**
   - Принимает список результатов от `download_one()`
   - Сохраняет каждый результат как JSON строку в файл
   - Формат: одна строка = один JSON объект
   - Использовать режим `'a'` (append) для добавления

2. **`read_manifest(manifest_path)`**
   - Читает JSONL файл
   - Возвращает список словарей
   - Обрабатывать ошибки парсинга (пропускать некорректные строки)

3. **`get_downloaded_urls(manifest_path)`**
   - Читает манифест
   - Возвращает `set` уже скачанных URL
   - Полезно для пропуска дубликатов

**Что делать:**
- Изучить JSONL формат
- Использовать `json.dumps()` и `json.loads()`
- Обработать ошибки (try/except для каждой строки)

#### 1.4. Улучшить `downloader/core.py` (или переделать `downloader.py`)

**Файл: `async_url_collector/downloader/core.py`** (новый файл)

Функции для реализации:

1. **`download_one(url, out_dir, session, semaphore, timeout, retries)`**
   - Асинхронная функция скачивания одного файла
   - Использовать `semaphore` для ограничения параллелизма
   - Реализовать ретраи с задержкой
   - Использовать `aiofiles` для записи
   - Вычислять SHA256 хеш файла
   - Возвращать словарь с метаданными

2. **`download_many(urls, out_dir, concurrency, timeout, retries)`**
   - Создаёт `asyncio.Semaphore(concurrency)`
   - Создаёт одну `aiohttp.ClientSession` для всех запросов
   - Запускает все задачи через `asyncio.gather()`
   - Возвращает список результатов

**Что делать:**
- Перенести логику из `downloader.py` в `core.py`
- Добавить Semaphore
- Добавить ретраи
- Заменить `open()` на `aiofiles.open()`
- Добавить вычисление SHA256

**Ключевые концепции:**
- `async with semaphore:` - ограничение параллелизма
- `async with aiofiles.open(...) as f:` - асинхронная запись
- `hashlib.sha256(data).hexdigest()` - вычисление хеша

---

## 📚 Этап 2: Универсальный парсер (Неделя 2)

### 🎓 Что изучать:

1. **BeautifulSoup - продвинутый парсинг** (1-2 часа)
   - Поиск по классам: `soup.find_all(class_='...')`
   - Поиск по атрибутам: `soup.find_all(attrs={'data-id': '...'})`
   - CSS селекторы: `soup.select('.class #id')`
   - Извлечение текста: `.get_text()`, `.string`
   - Ресурсы: документация BeautifulSoup, примеры парсинга

2. **Структура HTML** (30 минут)
   - Как найти метаданные в `<meta>` тегах
   - Open Graph теги (`og:image`, `og:title`)
   - Структура разных сайтов
   - Ресурсы: инспектировать реальные сайты в браузере (F12)

3. **urllib.parse** (20 минут)
   - `urlparse()` - разбор URL на части
   - `urljoin()` - объединение относительных и абсолютных URL
   - Ресурсы: документация Python urllib.parse

### ✍️ Что писать:

#### 2.1. Улучшить `parsers/base.py`

**Что улучшить:**
- ✅ Уже исправлена опечатка (этап 1)
- Добавить метод `extract_metadata(html, base_url)`:
  - Извлекать `<title>`
  - Извлекать Open Graph теги (`og:image`, `og:title`, `og:author`)
  - Извлекать дату из `<meta>` тегов
  - Возвращать словарь с метаданными

- Улучшить `extract_images()`:
  - Учитывать URL с параметрами (использовать `urlparse` и брать только путь)
  - Искать изображения в `<img src>`, `<img data-src>`, `<source srcset>`
  - Фильтровать по расширениям из `config.PARSER_CONFIG`

#### 2.2. Создать `parsers/generic.py`

**Файл: `async_url_collector/parsers/generic.py`**

Класс `GenericParser(BaseParser)`:

Методы:
1. **`async def parse_page(url)`**
   - Получает HTML через `fetch_html()`
   - Извлекает все изображения через `extract_images()`
   - Извлекает метаданные через `extract_metadata()`
   - Возвращает словарь: `{'images': [...], 'metadata': {...}}`

2. **`async def parse_multiple(urls)`**
   - Парсит несколько URL параллельно
   - Использует `asyncio.gather()`
   - Возвращает список результатов

**Что делать:**
- Наследоваться от `BaseParser`
- Использовать методы базового класса
- Добавить обработку ошибок

---

## 📚 Этап 3: Парсеры конкретных платформ (Неделя 3)

### 🎓 Что изучать:

1. **Структура конкретных платформ** (2-3 часа)
   - Открыть сайт в браузере
   - Использовать DevTools (F12) для изучения HTML
   - Найти, где хранятся изображения, автор, дата, теги
   - Ресурсы: инспектировать реальные страницы

2. **Обработка динамического контента** (опционально, 1-2 часа)
   - Некоторые сайты загружают контент через JavaScript
   - Для этого нужен Selenium/Playwright (позже, если нужно)
   - Пока фокус на статическом HTML

### ✍️ Что писать:

#### 3.1. Создать `parsers/pinterest.py`

**Файл: `async_url_collector/parsers/pinterest.py`**

Класс `PinterestParser(BaseParser)`:

Методы:
1. **`async def parse_pin(pin_url)`**
   - Парсит страницу пина
   - Извлекает изображение (обычно большое)
   - Извлекает автора, описание, теги
   - Возвращает метаданные

2. **`async def parse_board(board_url)`**
   - Парсит доску (список пинов)
   - Находит все пины на странице
   - Парсит каждый пин
   - Возвращает список изображений с метаданными

**Что делать:**
- Изучить структуру Pinterest в браузере
- Найти CSS селекторы для нужных элементов
- Реализовать специфичную логику парсинга

#### 3.2. Создать `parsers/telegram.py`

**Файл: `async_url_collector/parsers/telegram.py`**

Класс `TelegramParser(BaseParser)`:

**Важно:** Telegram требует авторизацию для доступа к контенту. Есть варианты:
- Парсить публичные веб-страницы Telegram (если есть)
- Использовать Telegram Bot API (если есть токен)
- Парсить экспортированные данные

Методы (в зависимости от выбранного подхода):
1. **`async def parse_channel(channel_url)`**
2. **`async def parse_message(message_url)`**

**Что делать:**
- Изучить, какой доступ есть к Telegram
- Выбрать подход
- Реализовать парсер

---

## 📚 Этап 4: Анализ данных (Неделя 4) - КЛЮЧЕВОЙ ЭТАП

### 🎓 Что изучать:

1. **Анализ данных в Python** (2-3 часа)
   - Группировка: `itertools.groupby()`, словари со списками
   - Статистика: `collections.Counter`, `statistics` модуль
   - Фильтрация: list comprehensions, `filter()`
   - Ресурсы: документация Python, примеры анализа данных

2. **Алгоритмы поиска паттернов** (1-2 часа)
   - Группировка по общим признакам
   - Поиск связей между данными
   - Ресурсы: базовые алгоритмы, примеры на Python

3. **Генерация отчётов** (1 час)
   - Форматирование JSON: `json.dumps(indent=2)`
   - Простой HTML: можно использовать шаблоны или просто строки
   - Ресурсы: документация json, примеры HTML генерации

### ✍️ Что писать:

#### 4.1. Создать `analyzer/duplicates.py`

**Файл: `async_url_collector/analyzer/duplicates.py`**

Функции:

1. **`find_duplicates(manifest_path)`**
   - Читает манифест
   - Группирует файлы по SHA256 хешу
   - Находит группы с >1 файлом (дубликаты)
   - Возвращает словарь: `{hash: [file1, file2, ...]}`

2. **`get_duplicate_stats(manifest_path)`**
   - Использует `find_duplicates()`
   - Подсчитывает статистику:
     - Количество уникальных файлов
     - Количество дубликатов
     - Объём сэкономленного места (если удалить дубликаты)
   - Возвращает словарь со статистикой

**Что делать:**
- Использовать `collections.defaultdict(list)` для группировки
- Читать манифест через `downloader.manifest.read_manifest()`
- Фильтровать только успешно скачанные файлы (`ok: True`)

#### 4.2. Создать `analyzer/metadata.py`

**Файл: `async_url_collector/analyzer/metadata.py`**

Функции:

1. **`analyze_file_sizes(manifest_path)`**
   - Статистика по размерам файлов
   - Минимум, максимум, среднее, медиана
   - Распределение по диапазонам

2. **`analyze_content_types(manifest_path)`**
   - Группировка по `content_type`
   - Подсчёт количества каждого типа
   - Топ типов файлов

3. **`analyze_timeline(manifest_path)`**
   - Группировка по датам (если есть в метаданных)
   - График активности скачиваний
   - Возвращает словарь: `{date: count}`

4. **`get_platform_stats(manifest_path)`**
   - Группировка по доменам URL
   - Подсчёт файлов с каждого домена
   - Топ платформ

**Что делать:**
- Использовать `collections.Counter` для подсчёта
- Использовать `statistics` модуль для средних значений
- Парсить даты из `timestamp` поля

#### 4.3. Создать `analyzer/patterns.py`

**Файл: `async_url_collector/analyzer/patterns.py`**

Функции:

1. **`analyze_url_patterns(manifest_path)`**
   - Парсит все URL из манифеста
   - Находит общие паттерны в структуре URL
   - Группирует по доменам, путям, параметрам
   - Возвращает статистику по паттернам

2. **`find_related_files(manifest_path, url)`**
   - Находит файлы, связанные с данным URL
   - По домену, по пути, по параметрам
   - Возвращает список связанных файлов

3. **`group_by_author(manifest_path)`**
   - Группирует файлы по автору (если есть в метаданных)
   - Возвращает словарь: `{author: [files]}`

**Что делать:**
- Использовать `urllib.parse` для разбора URL
- Искать паттерны через регулярные выражения (если нужно)
- Группировать данные по общим признакам

#### 4.4. Создать `analyzer/reporter.py`

**Файл: `async_url_collector/analyzer/reporter.py`**

Функции:

1. **`generate_json_report(manifest_path, output_path)`**
   - Собирает всю статистику из других модулей
   - Формирует большой JSON объект:
     ```json
     {
       "summary": {...},
       "duplicates": {...},
       "metadata": {...},
       "patterns": {...}
     }
     ```
   - Сохраняет в файл с красивым форматированием (`indent=2`)

2. **`generate_html_report(manifest_path, output_path)`** (опционально)
   - Генерирует простой HTML отчёт
   - Таблицы со статистикой
   - Можно использовать шаблоны или просто строки

**Что делать:**
- Импортировать все функции анализа
- Собрать результаты в один словарь
- Использовать `json.dumps()` для форматирования
- Сохранить в файл

---

## 📚 Этап 5: CLI интерфейс (Неделя 5)

### 🎓 Что изучать:

1. **`argparse` - подкоманды** (1-2 часа)
   - `add_subparsers()` - создание подкоманд
   - Позиционные и опциональные аргументы
   - Значения по умолчанию
   - Ресурсы: документация Python argparse, примеры с подкомандами

2. **`__main__.py`** (30 минут)
   - Как Python находит этот файл при `python -m package`
   - Структура файла
   - Ресурсы: документация Python packages

### ✍️ Что писать:

#### 5.1. Создать `__main__.py`

**Файл: `async_url_collector/__main__.py`**

Структура:

```python
import argparse
import asyncio
from pathlib import Path

def main():
    parser = argparse.ArgumentParser(
        description='Web Content Analyzer - сбор и анализ контента'
    )
    
    subparsers = parser.add_subparsers(dest='command', help='Команды')
    
    # Подкоманда download
    download_parser = subparsers.add_parser('download', help='Скачать файлы')
    download_parser.add_argument('urls_file', help='Файл с URL (по одному на строку)')
    download_parser.add_argument('--out', default='downloads', help='Папка для сохранения')
    download_parser.add_argument('--concurrency', type=int, default=8, help='Параллелизм')
    download_parser.add_argument('--timeout', type=int, default=30, help='Таймаут (сек)')
    download_parser.add_argument('--retries', type=int, default=3, help='Повторные попытки')
    
    # Подкоманда analyze
    analyze_parser = subparsers.add_parser('analyze', help='Анализировать манифест')
    analyze_parser.add_argument('manifest', help='Путь к manifest.jsonl')
    analyze_parser.add_argument('--format', choices=['json', 'html'], default='json')
    
    # Подкоманда report
    report_parser = subparsers.add_parser('report', help='Сгенерировать отчёт')
    report_parser.add_argument('manifest', help='Путь к manifest.jsonl')
    report_parser.add_argument('--output', help='Путь для сохранения отчёта')
    
    args = parser.parse_args()
    
    if args.command == 'download':
        asyncio.run(download_command(args))
    elif args.command == 'analyze':
        analyze_command(args)
    elif args.command == 'report':
        report_command(args)
    else:
        parser.print_help()

if __name__ == '__main__':
    main()
```

Функции для реализации:

1. **`async def download_command(args)`**
   - Читает URL из файла через `io_utils.read_urls()`
   - Вызывает `downloader.core.download_many()`
   - Сохраняет результаты в манифест через `manifest.save_to_manifest()`
   - Выводит статистику

2. **`def analyze_command(args)`**
   - Читает манифест
   - Вызывает функции анализа
   - Выводит результаты в консоль

3. **`def report_command(args)`**
   - Вызывает `analyzer.reporter.generate_json_report()`
   - Сохраняет отчёт

**Что делать:**
- Изучить `argparse` с подкомандами
- Реализовать каждую команду
- Добавить обработку ошибок
- Добавить красивый вывод (можно использовать `rich` библиотеку, опционально)

---

## 🎯 Итоговый чеклист проекта

### Этап 1: Базовая инфраструктура
- [ ] Исправлен `parsers/base.py` (опечатка)
- [ ] Исправлен `main.py` (импорты, переменные)
- [ ] Создан `config.py`
- [ ] Создан `downloader/manifest.py` (3 функции)
- [ ] Создан/улучшен `downloader/core.py` (2 функции с Semaphore и aiofiles)
- [ ] Обновлён `requirements.txt`

### Этап 2: Универсальный парсер
- [ ] Улучшен `parsers/base.py` (метод `extract_metadata`)
- [ ] Создан `parsers/generic.py` (класс `GenericParser`)

### Этап 3: Парсеры платформ
- [ ] Создан `parsers/pinterest.py`
- [ ] Создан `parsers/telegram.py` (или другой платформы)

### Этап 4: Анализ данных
- [ ] Создан `analyzer/duplicates.py` (2 функции)
- [ ] Создан `analyzer/metadata.py` (4 функции)
- [ ] Создан `analyzer/patterns.py` (3 функции)
- [ ] Создан `analyzer/reporter.py` (1-2 функции)

### Этап 5: CLI интерфейс
- [ ] Создан `__main__.py` с подкомандами
- [ ] Реализованы все команды (download, analyze, report)

---

## 📖 Рекомендуемый порядок изучения

1. **Неделя 1:** asyncio.Semaphore → aiofiles → JSONL → pathlib
2. **Неделя 2:** BeautifulSoup (продвинутый) → urllib.parse
3. **Неделя 3:** Инспектирование HTML в браузере → CSS селекторы
4. **Неделя 4:** collections.Counter → statistics → группировка данных
5. **Неделя 5:** argparse (подкоманды) → __main__.py

---

## 💡 Советы по обучению

1. **Не пытайтесь изучить всё сразу** - фокусируйтесь на текущем этапе
2. **Практикуйтесь на реальных примерах** - парсите реальные сайты
3. **Используйте DevTools** - изучайте HTML в браузере (F12)
4. **Тестируйте каждый модуль отдельно** - создавайте простые тестовые скрипты
5. **Читайте документацию** - она часто лучше, чем туториалы

---

## 🚀 Следующие шаги

**Начните с Этапа 1, пункт 1.1** - исправьте существующий код, чтобы он запускался.

После каждого этапа - тестируйте, проверяйте, что всё работает.

Удачи! 🎉

