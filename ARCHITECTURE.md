# Архитектура проекта

## 📁 Структура файлов

```
async_url_collector/
├── __main__.py              # CLI точка входа (argparse)
├── config.py                # Настройки проекта
├── main.py                  # Старый main.py (можно удалить после переноса логики)
│
├── downloader/              # Модуль скачивания
│   ├── __init__.py
│   ├── core.py             # Основная логика (download_one, download_many)
│   ├── downloader.py        # Старый файл (можно удалить после переноса)
│   ├── io_utils.py         # Чтение/запись файлов
│   └── manifest.py         # Работа с манифестом (JSONL)
│
├── parsers/                 # Парсеры для разных платформ
│   ├── __init__.py
│   ├── base.py             # Базовый класс парсера
│   ├── generic.py          # Универсальный парсер
│   ├── pinterest.py         # Парсер Pinterest
│   └── telegram.py         # Парсер Telegram
│
├── analyzer/                # Модуль анализа данных
│   ├── __init__.py
│   ├── duplicates.py       # Поиск дубликатов
│   ├── metadata.py         # Анализ метаданных
│   ├── patterns.py         # Поиск паттернов
│   └── reporter.py         # Генерация отчётов
│
└── utils/                   # Вспомогательные функции
    ├── __init__.py
    └── exif.py             # Извлечение EXIF (опционально)
```

## 🔗 Зависимости между модулями

```
__main__.py
  ├── downloader.core (download_many)
  ├── downloader.io_utils (read_urls)
  ├── downloader.manifest (save_to_manifest)
  └── analyzer.reporter (generate_json_report)

downloader.core
  └── downloader.manifest (для сохранения результатов)

analyzer.*
  └── downloader.manifest (read_manifest)

parsers.*
  └── parsers.base (наследование)
```

## 📝 Статус файлов

### ✅ Созданы и готовы к реализации:
- `config.py` - настройки (готов к использованию)
- `__main__.py` - структура CLI (нужно реализовать команды)
- `downloader/manifest.py` - функции с TODO
- `downloader/core.py` - функции с TODO
- `analyzer/*.py` - все модули анализа с TODO
- `parsers/generic.py` - универсальный парсер с TODO
- `parsers/pinterest.py` - парсер Pinterest с TODO
- `parsers/telegram.py` - парсер Telegram с TODO
- `utils/exif.py` - EXIF утилита с TODO

### ⚠️ Требуют исправления:
- `parsers/base.py` - исправить опечатку 'html,parser' → 'html.parser'
- `main.py` - исправить импорты и переменные

### 📦 Обновлено:
- `requirements.txt` - добавлен beautifulsoup4

## 🎯 Следующие шаги

1. **Исправить существующий код** (Этап 1.1 из PLAN.md)
   - `parsers/base.py` - опечатка
   - `main.py` - импорты и переменные

2. **Реализовать базовую инфраструктуру** (Этап 1)
   - `downloader/manifest.py` - все функции
   - `downloader/core.py` - download_one и download_many

3. **Следовать плану** из PLAN.md по порядку

## 💡 Примечания

- Все файлы содержат подробные TODO комментарии с инструкциями
- Каждая функция имеет docstring с описанием
- Импорты закомментированы (раскомментируйте по мере реализации)
- Структура готова к расширению новыми платформами

